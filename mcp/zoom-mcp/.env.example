# The MCP server proxies to the zoom-meeting edge function; it holds no Zoom creds.
SUPABASE_URL=https://peohauhwquuvghrpmotf.supabase.co
# Project Settings → API → service_role. SECRET. Never expose in a browser.
SUPABASE_SERVICE_ROLE_KEY=
