# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
.output
.vinxi
.tanstack/**
.nitro
*.local

# Environment variables (never commit real secrets)
.env
.env.*
!.env.example

# Local bun lockfile — npm (package-lock.json) is the canonical lockfile
bun.lock

# Wrangler / Cloudflare
.wrangler/
.dev.vars

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
.vercel
supabase/.temp/

# Python (scripts/ingest-paper)
__pycache__/
*.pyc

# Exam papers. Board copyright, held locally for building the exemplar library —
# never committed. The rows extracted from them live in the database instead.
# The README stays, so the workflow is documented where the work happens.
papers/*
!papers/README.md
